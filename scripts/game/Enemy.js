class Enemy extends MyAnimation {
  constructor(
    matriz, 
    image, 
    positionX,
    characterWidth, 
    characterHeight, 
    spriteWidth, 
    spriteHeight,
  ) {
    super(
      matriz, 
      image, 
      positionX,
      characterWidth, 
      characterHeight, 
      spriteWidth, 
      spriteHeight,
    );
  }
}